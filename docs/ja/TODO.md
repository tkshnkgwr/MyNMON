# タスク管理 (TODO.md)

[English](../en/TODO.md) | **日本語版**

本ドキュメントは、`MyNMON` の開発タスクの完了状況および今後のロードマップを管理するファイルです。

---

## 1. 完了済みタスク (Done)

- [x] **基本モニター機能の実装**: CPU(全体/コア別), メモリ, ディスク, ネットワーク, プロセスの各モニター。
- [x] **CUI 画面保護・フリッカー防止**: 80x20 画面サイズチェック、上書き描画、行末消去。
- [x] **多重起動防止**: `common_lib::check_single_instance` による Windows Named Mutex 制御。
- [x] **インタラクティブ制御**: セクション表示トグル、プロセス名フィルタリング、更新間隔変更。
- [x] **モジュール分割**: `main.rs`, `state.rs`, `ui.rs`, `utils.rs` へのコード分割。
- [x] **Cargo Features による機能分離**: モジュールごとのトグル（`cpu`, `mem`, `disk`, `net`, `proc`, `diff`）および条件付きコンパイル対応。

---

## 2. 直近の予定タスク (Todo)

- [ ] **GPU モニター機能の検討**: NVIDIA GPU (NVML) 等のサポートをオプショナル Feature フラグとして追加の検討。
- [ ] **ログ出力機能 (CSV/JSON)**: 指定間隔でメトリクスをバックグラウンド記録する機能。
