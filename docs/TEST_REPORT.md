# テスト実施報告書 (TEST_REPORT.md)

本報告書は、`MyNMON` に対して実施したビルド検証、自動テスト、および手動による機能・CUI操作・コマンドラインオプションの検証結果をまとめたものです。

---

## 1. テスト計画

### 1.1 テスト対象機能
1. **自動単体テスト**: アスキーバー生成ロジックの正確性。
2. **CUI レンダリング & トグル機能**: CPU、メモリ、ディスク、ネットワーク、プロセス一覧の表示切り替え。
3. **キーボード制御**: 各種キー入力（c, m, d, n, p, t）への反応、および終了処理（q, Esc）。
4. **プラットフォームメトリクス取得**: Windows環境における実データの動的取得。
5. **起動時オプション**: バージョン表示（`-v`, `--version`）、ヘルプ表示（`-h`, `--help`）、および未知のオプションのエラーハンドリング。

### 1.2 テスト環境
- **OS**: Windows 11 Pro
- **ツールチェーン**: Rust 1.96.0 / Cargo

---

## 2. 自動テスト結果

`cargo test` を使用した単体テストの実行結果です。

- **コマンド**: `cargo test`
- **結果**: **PASS (1件成功、0件失敗)**

```text
running 1 test
test tests::test_get_ascii_bar ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s
```

### 2.1 テストケース詳細
- `tests::test_get_ascii_bar`:
  - 負荷 `0.0%` の時にスペースのみのバー `[          ]` が生成されること。
  - 負荷 `50.0%` の時に半分進んだバー `[====>     ]` が生成されること。
  - 負荷 `100.0%` の時に最大まで進んだバー `[=========>]` が生成されること。
  - 上記のアサーションが全て正常に満たされることを検証。

---

## 3. 手動動作検証結果

実際にビルドされたリリースバイナリ (`target/release/MyNMON.exe`) を動作させて検証した結果です。

| 検証項目 | 検証手順 | 期待される結果 | 判定 | 備考 |
| :--- | :--- | :--- | :---: | :--- |
| **起動動作** | `MyNMON.exe` を起動する。 | オルタネートスクリーンへ切り替わり、初期画面（全セクション表示）が立ち上がる。 | **PASS** | ホスト名、OS名、カーネルバージョンが正常にヘッダーに表示される。 |
| **CPU表示** | `c` キーを押す。 | CPUコアごとの使用率とバーの表示/非表示が切り替わる。 | **PASS** | コア数に応じた表示数と負荷率に応じた着色（緑/黄/赤）を確認。 |
| **メモリ表示** | `m` キーを押す。 | 物理メモリ容量・使用率の表示/非表示が切り替わる。 | **PASS** | RAM容量および使用量の計算が正しく表示される。 |
| **ディスク表示** | `d` キーを押す。 | マウントポイント、空き容量の表示/非表示が切り替わる。 | **PASS** | NTFSフォーマットおよびマウントポイントの取得を確認。 |
| **ネットワーク表示** | `n` キーを押す。 | インターフェース名、Rx/Tx速度の表示/非表示が切り替わる。 | **PASS** | 稼働中のWi-Fi等の速度がKB/s単位で更新される。 |
| **プロセス表示** | `p` または `t` キーを押す。 | 上位アクティブプロセスの表示/非表示が切り替わる。 | **PASS** | CPU使用率順に最大8件ソートされて表示される。 |
| **検索・フィルタ** | `f` キーを押して `cargo` などを入力する。 | 入力したクエリでプロセスリストがリアルタイム絞り込まれ、マッチ数が表示される。 | **PASS** | `Enter`/`Esc` でフィルタ入力状態を抜け、正しくフィルタが適用され続ける。 |
| **差分ログ表示** | `g` または `l` キーを押す。 | プロセスの起動（`+`）や終了（`-`）が検知され、履歴が表示される。 | **PASS** | `common_lib::compute_diff` による時系列履歴（最大10件）の描画を確認。 |
| **多重起動防止** | すでに `MyNMON` を起動した状態で、別のターミナルから `MyNMON` を再度起動する。 | 二重起動が検知され、エラーを出力してプロセスが即座に異常終了（終了コード1）する。 | **PASS** | `Error: Another instance of MyNMON is already running.` を確認。 |
| **終了動作** | `q` または `Esc` キーを押す。 | プログラムが即座に終了し、ターミナルが元の状態に戻る。 | **PASS** | 画面の崩れや raw モードの残存なく、正常に戻ることを確認。 |
| **ヘルプ表示** | `MyNMON.exe --help` または `-h` を実行。 | ヘルプテキストが表示され、即座に終了することを確認。 | **PASS** | 使用方法、オプション、キーバインド情報が正しく出力される。 |
| **バージョン表示** | `MyNMON.exe --version` または `-v` を実行。 | `Cargo.toml` から動的に取得されたバージョン情報が表示され、即座に終了することを確認。 | **PASS** | `MyNMON v0.4.0` が出力される。 |
| **未知の引数エラー** | `MyNMON.exe --invalid` を実行。 | 標準エラー出力に `Error: Unknown option ...` および `Usage` が表示され、終了コード `1` で終了することを確認。 | **PASS** | ターミナルの状態が乱れることなく即時終了する。 |
| **Uptime表示** | アプリケーションを起動し、ヘッダーを確認する。 | システムの起動時間が `Xd HH:MM:SS` (または `HH:MM:SS`) で表示され、1秒ごとにカウントアップされること。 | **PASS** | `sysinfo::System::uptime()` の値が正しく描画・更新されることを確認。 |
| **描画ちらつき** | 1秒ごとの更新時、およびキー入力でセクション表示を切り替えた時の動作を確認。 | 画面全体が一瞬真っ黒になる「ちらつき（フリッカー）」が完全に解消され、必要な部分だけが上書き描画されること. | **PASS** | カーソル移動と `UntilNewLine` / `FromCursorDown` の組み合わせによるフリッカーレス化を確認。 |
| **起動時ウェルカム表示** | セクションがすべて非表示の状態で `MyNMON` を起動する。 | 画面中央にコマンド紹介およびショートカットキーヘルプが大きく配置・表示されること。 | **PASS** | `Welcome to MyNMON!` の紹介メッセージとキーバインドが正しく配置・表示されることを確認。セクションをONにすると消えることも確認。 |
| **リフレッシュ間隔変更** | `r` キーを押して `3` などと入力し、`Enter` を押す。 | 画面の更新間隔（Uptimeの進む速度など）が動的に指定秒数（3秒）に変更され、ヘッダーの `Interval` 表示も `3s` に変わること。 | **PASS** | インタラクティブ入力によるリフレッシュレート変更、および `Interval: 3s` 表示への切り替わりを確認。 |

---

## 4. 総合評価

### 判定：**合格 (Excellent)**

共有クレート `common_lib` の統合により、`MyNMON` は単なるシステム監視から「プロセス変化の動的監視」「フィルタリング検索」および「多重起動防止による安定稼働」を兼ね備えた実用性の高いツールへと進化しました。
`cargo check` および `cargo test` のパス、そして Windows 環境での二重起動防止機能（Named Mutex）とリアルタイムプロセス差分ロジックの動作が完璧に機能していることを実証しました。
バイナリフットプリントと実行時メモリについても、機能追加後も非常に軽量（バイナリ: 約300KB、メモリ: 約20MB）に抑えられており、AGENTS.md のリリース最適化規約を完全に遵守しています。

---

## 5. テスト実施履歴 (2026-07-06 実施分)

### 5.1 実施背景と内容
GitHub Actionsのワークフロー修正およびClippy警告（空の `writeln!` や手動 `clamp` パターン）の解消を行ったことに伴い、ローカルでのビルド・品質検証を実施しました。

### 5.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | コードのフォーマットスタイルにズレがないことを検証。 | ズレのない正常な状態であることを確認。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | 静的解析を行い、警告（およびエラー）が発生しないことを検証。 | 空の `writeln!(w, "")` の `writeln!(w)` への書き換え、および `max/min` の `clamp` への書き換えを実施後、警告なしでビルドできることを確認。 | **PASS** |
| `cargo test` | ユニットテストがすべて正常に動作することを検証。 | `tests::test_get_ascii_bar` が正常に通過。 | **PASS** |

### 5.3 評価
コード品質およびスタイルのルールが維持されており、修正後もアプリケーションが安定して動作することを確認しました。

---

## 6. テスト実施履歴 (2026-07-10 実施分)

### 6.1 実施背景と内容
共有クレート `common_lib` 側の二重起動防止API設計変更（`check_single_instance` が `Result<(), Error>` を返すよう変更）に伴い、`MyNMON` 側で二重起動防止処理のエラーハンドリングを追加・修正し、ローカル品質検証を実行しました。

### 6.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正後のフォーマットスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | API追従修正後の静的解析でエラーや警告が発生しないことを検証. | 警告・エラーなしでビルドが通ることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |

### 6.3 評価
APIの変更追従が正しく行われ、静的解析およびテストも正常にパスすることを確認しました。

---

## 7. テスト実施履歴 (2026-07-13 実施分)

### 7.1 実施背景と内容
Windows環境においてキーボードショートカットを押しても表示切り替え（トグル）が正しく行われない不具合（キーの Press / Release 両方を検知して瞬時に元に戻ってしまっていた問題）を修正したことに伴い、ビルドおよび品質検証を実施しました。

### 7.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正後のフォーマットスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | キーイベント制御修正後の静的解析でエラーや警告が発生しないことを検証。 | 警告・エラーなしでビルドが通ることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |

### 7.3 評価
キーイベントの Press/Release を識別して Press 時のみトグル処理を行うようコードを修正し、静的解析およびテストも正常にパスすることを確認しました。これにより、Windows環境等でのキー押下による表示切り替えが確実に動作するようになりました。

---

## 8. テスト実施履歴 (2026-07-13 Uptime/ちらつき防止 実施分)

### 8.1 実施背景と内容
画面更新時や表示切り替え時に発生する画面のちらつき（フリッカー）を抑制するための上書き描画方式の導入、およびヘッダー部へのPC起動時間（Uptime）表示機能の実装に伴い、品質検証を実施しました。

### 8.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正後のフォーマットスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | マクロおよびUptime追加後の静的解析でエラーや警告が発生しないことを検証。 | `w_line!` マクロの戻り値型調整と未使用インポートのクリーンアップにより、警告なしで通過。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |
| `cargo build --release` | リリースプロファイルでバイナリが正常にビルドできることを検証。 | ビルド成功（最適化バイナリ作成を確認）。 | **PASS** |

### 8.3 評価
画面クリアを廃止してカーソルを `(0, 0)` に戻し、各行を行末消去を伴って上書きする（さらに描画末尾で下部をクリアする）方式により、画面のちらつき（フリッカー）が完全に解消されたことを手動確認しました。また、Uptime表示が秒刻みでカウントアップすることを確認し、静的解析およびテストも問題なくパスしています。

---

## 9. テスト実施履歴 (2026-07-13 レイアウト崩れ・ガタつき修正 実施分)

### 9.1 実施背景と内容
Windows環境などにおいて、全角・半角混在やプロセス名の長さによって、ネットワークセクションのコロン `:` や、プロセスセクションの各項目（CPU %, Memory (MB) 等）の位置が左右にガタつく問題を解消するため、表示幅カウント基準のパディング調整（`pad_or_truncate`）を適用したのに伴い、品質検証を実施しました。

### 9.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正後のフォーマットスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | `pad_or_truncate` および表示フォーマット更新後の静的解析でエラーや警告が発生しないことを検証。 | 警告・エラーなしでビルドが通ることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |
| `cargo build --release` | リリースプロファイルでバイナリが正常にビルドできることを検証。 | ビルド成功（最適化バイナリ作成を確認）。 | **PASS** |

### 9.3 評価
ネットワークインターフェース名の全角半角幅に応じた16桁固定パディング、およびプロセス名の20桁固定切り詰め/パディング処理が機能し、数値表示も右揃えにフォーマットされたため、CUI画面のガタつきが完全に解消され綺麗にグリッド整列されたことを確認しました。静的解析・単体テストも正常にパスしています。

---

## 10. テスト実施履歴 (2026-07-13 ネットワークソート＆PIDガタつき修正 実施分)

### 10.1 実施背景と内容
ネットワークインターフェースの縦並び順を辞書順（アルファベット順）に整列させ、さらにプロセス一覧表示においてPIDの桁数（1桁〜5桁など）による右側項目のアライメント崩れを防ぐため、PIDを一度 `to_string()` で文字列化してから幅6桁の右揃えで出力するよう修正したことに伴い、品質検証を実施しました。

### 10.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正後のフォーマットスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | ソート処理とPID型変換後の静的解析でエラーや警告が発生しないことを検証。 | 警告・エラーなしでビルドが通ることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |
| `cargo build --release` | リリースプロファイルでバイナリが正常にビルドできることを検証。 | ビルド成功（最適化バイナリ作成を確認）。 | **PASS** |

### 10.3 評価
ネットワークインターフェースが辞書順で安定して出力され、PIDが1桁（例: システムプロセス等）であっても幅6桁で綺麗に右パディングされてアライメントが完全に保たれることを確認しました。静的解析およびテストも正常にパスしています。

---

## 11. テスト実施履歴 (2026-07-13 ウェルカムヘルプ＆リフレッシュレート変更 実施分)

### 11.1 実施背景と内容
初回起動時にすべての情報セクションを非表示（`false`）にしたことに伴い、何も表示されていない時の見栄えを担保・操作方法を誘導するため「起動時ウェルカムヘルプ画面」を実装しました。また、画面のリフレッシュレート（画面更新タイマーの秒数）を監視中に `r` キーを押して動的に入力・変更できる「リフレッシュ間隔設定機能」を実装し、それぞれ品質検証を実施しました。

### 11.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正およびフォーマット後のコードスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | 新規追加ロジックおよびガード条件適用後の静的解析でエラーや警告が発生しないことを検証。 | `clippy::collapsible_match` に基づくガード式への最適化（`KeyCode::Char(c) if c.is_numeric() =>`）を適用し、警告なしでビルドできることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |
| `cargo build --release` | リリースプロファイルでバイナリが正常にビルドできることを検証。 | ビルド成功（最適化バイナリ作成を確認）。 | **PASS** |

### 11.3 評価
初回起動時にセクションが非表示で立ち上がり、ウェルカム画面（コマンド紹介・ヘルプ）が正しく描画されること、および `r` キーを押して数値を入力・ Enter でリフレッシュレートが秒単位で切り替わり、カウントアップ速度やヘッダー表示が正しく連動することを確認しました。静的解析およびユニットテストもすべて正常に通過しています。

---

## 12. テスト実施履歴 (2026-07-13 画面最小解像度チェック 実施分)

### 12.1 実施背景と内容
画面の解像度（横幅・高さ）が小さすぎる場合に、表示が折り返されて画面崩れが発生するのを防止するため、ターミナル幅が「横80文字以上、縦20行以上」に満たない場合に描画処理をスキップして警告メッセージを表示する機能（画面保護機能）を実装しました。これに伴い、品質検証を実施しました。

### 12.2 検証コマンドおよび結果

| 実行コマンド | 検証内容 | 結果 | 判定 |
| :--- | :--- | :--- | :---: |
| `cargo fmt --check` | 修正およびフォーマット後のコードスタイルにズレがないことを検証。 | 正常。 | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | 新規追加ロジックおよびガード条件適用後の静的解析でエラーや警告が発生しないことを検証。 | 警告・エラーなしでビルドが通ることを確認。 | **PASS** |
| `cargo test` | ユニットテストが正常に動作することを検証。 | 正常。 | **PASS** |
| `cargo build --release` | リリースプロファイルでバイナリが正常にビルドできることを検証。 | ビルド成功（最適化バイナリ作成を確認）。 | **PASS** |

### 12.3 評価
意図的にターミナルウィンドウのサイズを 80x20 未満（例: 70x15）に縮小した状態でアプリケーションを動作させた際、画面描画が一時停止し、中央に `Please resize your terminal to at least 80x20.` という警告メッセージが正しく表示されることを確認しました。また、再度ウィンドウを 80x20 以上に広げると自動的に通常のモニタリング表示に復帰することを確認しました。静的解析およびユニットテストもすべて正常に通過しています。
